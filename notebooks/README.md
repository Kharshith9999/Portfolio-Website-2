# Project notebooks

Add the corresponding Jupyter notebooks here when they are ready. The project cards automatically open a notebook once it exists at its assigned path:

- `creditguard.ipynb`
- `predictai.ipynb`
- `pulseboard.ipynb`
- `agentiq.ipynb`

Keep these filenames so the portfolio links continue to work without any code changes.
